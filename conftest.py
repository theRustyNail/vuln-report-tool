# This file is intentionally almost empty.
# Its presence at the repo root tells pytest to add this directory to the
# import path, so tests can import the vulnreport package. Shared test
# fixtures can also live here later.
